<Window x:Class="FileCourier.Setup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FileCourier Setup" Height="520" Width="720"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="White" FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="40,45,40,0">
            <!-- Header -->
            <TextBlock Text="Install FileCourier" 
                       FontSize="32" Foreground="#003399" Margin="0,0,0,35" FontWeight="Regular"/>
            
            <!-- Increased font for label -->
            <TextBlock Text="Installation Folder:" 
                       FontSize="18" Margin="0,0,0,15" FontWeight="Regular"/>
            
            <!-- Path Selection Row - increased height and font -->
            <Grid Height="48">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PathTextBox" VerticalContentAlignment="Center" Padding="12,2" FontSize="18" BorderBrush="#FFCCCCCC" FontFamily="Segoe UI"/>
                <Button Grid.Column="1" Content="Browse..." Width="120" Margin="15,0,0,0" Click="Browse_Click" Background="White" FontSize="18" FontFamily="Segoe UI"/>
            </Grid>
            
            <TextBlock x:Name="StatusTextBlock" Margin="0,40,0,10" Foreground="DimGray" TextWrapping="Wrap" FontSize="14" FontFamily="Segoe UI"/>
            <ProgressBar x:Name="InstallProgressBar" Height="20" Visibility="Collapsed"/>
        </StackPanel>

        <!-- Footer Panel - increased button size and font -->
        <Border Grid.Row="1" Background="#F3F3F3" Padding="30,20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="InstallButton" Content="Install" Width="140" Height="45" Margin="0,0,15,0" Click="Install_Click" IsDefault="True" FontSize="18" FontFamily="Segoe UI"/>
                <Button Content="Cancel" Width="140" Height="45" Click="Cancel_Click" IsCancel="True" FontSize="18" FontFamily="Segoe UI"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
